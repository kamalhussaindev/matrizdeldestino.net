---
import Card from '../ui/Card.astro';
import Icon from '../ui/Icon.astro';

const signals = [
  'Calculation runs directly in your browser.',
  'No account needed.',
  'The calculation method is documented step by step.',
  'Your date of birth is never stored on our servers.',
];
---

<section class="mx-auto max-w-5xl px-4 py-16 sm:px-6">
  <h2 class="text-center font-heading text-2xl font-bold text-ink sm:text-3xl">Why Trust Arcania</h2>
  <div class="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
    {
      signals.map((text) => (
        <Card class="text-center">
          <div class="mx-auto flex h-10 w-10 items-center justify-center rounded-full bg-surface-alt text-primary">
            <Icon name="check" class="h-5 w-5" />
          </div>
          <p class="mt-3 text-sm text-ink-muted">{text}</p>
        </Card>
      ))
    }
  </div>
</section>
