# Copy to .env (local) or set in your deploy platform's environment variables.
# Leave unset to keep analytics off — the site works fine either way.

# Google Analytics 4 measurement ID (e.g. G-XXXXXXXXXX). Only takes effect in
# production builds (npm run build); has no effect in npm run dev.
PUBLIC_GA_MEASUREMENT_ID=
